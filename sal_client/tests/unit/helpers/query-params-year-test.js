import { queryParamsYear } from '../../../helpers/query-params-year';
import { module, test } from 'qunit';

module('Unit | Helper | query params year');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = queryParamsYear(42);
  assert.ok(result);
});
