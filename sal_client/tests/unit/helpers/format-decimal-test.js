import { formatDecimal } from '../../../helpers/format-decimal';
import { module, test } from 'qunit';

module('Unit | Helper | format decimal');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDecimal(42);
  assert.ok(result);
});
